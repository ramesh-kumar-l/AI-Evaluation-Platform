# TEMPLATE — replace base64 values before deploying.
# Generate: echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: aep-secrets
  namespace: aep
type: Opaque
data:
  AEP_DATABASE_URL: <BASE64_DATABASE_URL>
  AEP_ADMIN_SECRET: <BASE64_ADMIN_SECRET>
