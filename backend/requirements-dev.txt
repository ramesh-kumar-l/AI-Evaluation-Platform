# Development, CI, and optional observability dependencies.
# Includes production/runtime requirements first.

-r requirements.txt

# Observability extras. Keep aligned with [project.optional-dependencies].otel.
opentelemetry-sdk>=1.29
opentelemetry-exporter-otlp-proto-http>=1.29
opentelemetry-instrumentation-fastapi>=0.50b0

# Developer tooling. Keep aligned with [project.optional-dependencies].dev.
pytest>=9.0.3
ruff>=0.15.15
mypy>=2.1
