# Metric Governance (memory)

Definitions, semantics, and confidence interpretation for every metric. A metric is only usable once
its definition is recorded here so results stay explainable.

Populated from **Phase 3** (metric framework + catalog).
