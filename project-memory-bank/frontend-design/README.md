# Frontend Design (memory)

Design tokens, component inventory, IA, and trust-UI rules. The **Trust-First UI** field checklist
(versions, timestamp, method, metric definitions, confidence, approval status, audit history) is the
contract every result view must satisfy.

Design tokens established in **Phase 0**; trust components built in **Phase 4**.
